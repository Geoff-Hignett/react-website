const SectionBanner = ({ title }) => {
	return (
		<div className="bg-grey h-60 flex items-center justify-center">
			<h1 className="text-3xl uppercase font-bold font-poppins">{title}</h1>
		</div>
	);
};

export default SectionBanner;
